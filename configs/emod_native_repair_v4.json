{
  "version": "emod_native_repair_v4_20260903",
  "motivation": "Training-only counterfactual identified classifier ELU saturation after an oversized Adam update.",
  "single_algorithm_change_from_v3": "classifier learning rate 0.001 -> 0.00001",
  "head_lr": 0.00001,
  "backbone_lr": 0.0005,
  "optimizer": "Adam",
  "weight_decay": 0.0001,
  "gradient_clip": 1.0,
  "label_smoothing": 0.1,
  "batch_size": 64,
  "epochs": 100,
  "seed": 2025,
  "scheduler": "CosineAnnealingLR T_max=100 eta_min=1e-8",
  "model_selection": "maximum validation kappa, last tie",
  "test_evaluation": "one evaluation after the fixed full training budget and validation checkpoint selection",
  "scope": "Repair native learning using unchanged official architecture and prepared data; not a five-seed reproduction or unseen-participant affect validation",
  "no_test_based_retry": true
}
